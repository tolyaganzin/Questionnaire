import { OrderByAnsweredFlagPipe } from './order-by-answered-flag.pipe';

describe('OrderByAnsweredFlagPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByAnsweredFlagPipe();
    expect(pipe).toBeTruthy();
  });
});
